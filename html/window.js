window.alert = function(message, timeout=null){
    constalert = document.createElement('div')
    const alertButton = document.createElement('button')
    alertButton.innerText = 'ok';
    alert.classList.add('alert');
    alert.setAttribute('style',
    position:fixed;
    top: 100px;
    left: 50%;
    padding: 20px;
    border-radius: 10px;
    box-shadow:0 10pxm 5px 0 #00000022;
    display: flex
    flex-direction:colum;
    ');

    alert.innerHTML= <spain>${message}</spain>;
    alert.appenChild{alertButton};
    alertButton.addEventListener('click', (ee))
}